"use strict"; // this statement treat all js code in newer version.

//alert( 3 + 3 ) //we are using node.js not browser so this will not run simply as in browser.

console.log( 3 
    +3 ) //code readability should be high 
    
    let name = "Shyam"
    let age = 21 
    let isLoggedIn = false
    let state;
     
   /* primitive data types
   Number
   Null //stand alone value
   String
   Symbol   //mainly used in react to find unique components
   Undefined
   Bollean // true/false
   BigInt //used to large values mainly in stockmarket or bank etc.
   */

   //Non primitive data types 
   //object

   console.log(typeof isLoggedIn)